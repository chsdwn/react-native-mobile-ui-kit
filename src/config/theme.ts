const colors = {
  grayLight: '#f6f6f6',
  gray: '#e8e8e8',
  grayDark: '#bdbdbd',
  grayDarker: '#666666',
  green: '#5db075',
  black: '#000',
  white: '#fff',
};

const fontSize = {
  title: 36,
  content: 16,
  label: 14,
};

const roundness = {
  default: 8,
  full: 9999,
};

const spacing = {
  xs: 4,
  sm: 8,
  md: 16,
  lg: 32,
};

export const theme = {
  colors,
  fontSize,
  roundness,
  spacing,
};
